# change-lens
A working, fully offline  system that searches satellite imagery by meaning and detects real change over time (not noise). Judged on: retrieval accuracy vs held-out queries, change-detection precision vs held-out labeled cases, incremental indexing, and a reproducible report with scale/latency/provenance metrics.
