# Python
.venv/
__pycache__/
*.py[cod]

# Node
frontend/node_modules/
frontend/dist/

# Data: the SECOND dataset is a junction to C:\dataa\second, not repo content.
data/second
data/analyzed/
data/class_weights.json
uploads/

# Checkpoints. weights/resnet18_imagenet.pt IS committed (see below) so the demo
# never needs the network; the trained checkpoints are too large for git.
weights/*.pt
!weights/resnet18_imagenet.pt
weights/run1_baseline/
# history.json and final_metrics.json ARE committed: small, and they back the README numbers.

# Logs
*.log
# Run 4 was abandoned; only smoke-test checkpoints (~295 MB each, over GitHub's limit).
weights/run4/
data/users.json
data/.jwt_secret
